<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Board Algo | The Future of Boards</title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Outfit:wght@500;700;800&display=swap" rel="stylesheet">

    <script src="https://cdn.tailwindcss.com"></script>
    
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
    <script src="https://unpkg.com/lucide@latest"></script>

    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    colors: {
                        brand: {
                            black: '#020617', // Obsidian
                            surface: '#0f172a', // Slate 900
                            border: '#1e293b',
                            primary: '#6366f1', // Indigo
                            accent: '#8b5cf6',  // Violet
                        }
                    },
                    fontFamily: {
                        sans: ['Inter', 'sans-serif'],
                        display: ['Outfit', 'sans-serif'],
                    },
                    backgroundImage: {
                        'gradient-glow': 'radial-gradient(circle at center, rgba(99, 102, 241, 0.15) 0%, rgba(2, 6, 23, 0) 70%)',
                    },
                    animation: {
                        'cursor-blink': 'blink 1s step-end infinite',
                    },
                    keyframes: {
                        blink: {
                            '0%, 100%': { opacity: '1' },
                            '50%': { opacity: '0' },
                        }
                    }
                }
            }
        }
    </script>

    <style>
        /* FORCE DARK MODE */
        body {
            background-color: #020617;
            color: #f8fafc;
            overflow-x: hidden;
        }

        /* 3D Background */
        #canvas-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100vw;
            height: 100vh;
            z-index: 0;
            pointer-events: none;
        }

        /* Glassmorphism */
        .glass {
            background: rgba(15, 23, 42, 0.6);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.08);
            transition: all 0.3s ease;
        }

        /* Mouse Glow Effect for Cards */
        .glow-card {
            position: relative;
            overflow: hidden;
        }
        .glow-card::before {
            content: "";
            position: absolute;
            top: var(--y, -1000px);
            left: var(--x, -1000px);
            width: 400px;
            height: 400px;
            background: radial-gradient(circle, rgba(99, 102, 241, 0.15) 0%, transparent 70%);
            transform: translate(-50%, -50%);
            pointer-events: none;
            z-index: 0;
            transition: opacity 0.5s;
        }
        .glow-card:hover::before {
            opacity: 1;
        }

        /* Loader */
        .loader-screen {
            background: #020617;
            z-index: 9999;
        }
        
        /* Custom Scrollbar */
        ::-webkit-scrollbar { width: 8px; }
        ::-webkit-scrollbar-track { background: #020617; }
        ::-webkit-scrollbar-thumb { background: #1e293b; border-radius: 4px; }
        ::-webkit-scrollbar-thumb:hover { background: #6366f1; }
    </style>
</head>
<body class="selection:bg-brand-primary selection:text-white">

    <div id="loader" class="loader-screen fixed inset-0 flex flex-col items-center justify-center">
        <div class="w-64 relative">
            <div class="h-1 w-full bg-brand-border rounded-full overflow-hidden mb-4">
                <div id="loader-bar" class="h-full w-0 bg-brand-primary shadow-[0_0_15px_rgba(99,102,241,0.8)]"></div>
            </div>
            <div class="flex justify-between font-mono text-xs text-brand-primary">
                <span>SYSTEM BOOT</span>
                <span id="loader-percent">0%</span>
            </div>
            <div class="text-xs font-mono text-slate-600 mt-2 text-center" id="loader-text">Initializing AI Core...</div>
        </div>
    </div>

    <div id="canvas-container"></div>

    <nav class="fixed top-0 w-full z-50 border-b border-white/5 bg-brand-black/80 backdrop-blur-md">
        <div class="max-w-7xl mx-auto px-6 h-20 flex justify-between items-center">
            
            <a href="#" class="flex items-center gap-3 group">
                <img id="img-logo" src="logo.png" alt="BOARD Algo" class="h-8 w-auto object-contain hidden">
                <span id="text-logo" class="text-2xl font-display font-bold tracking-tight text-white group-hover:text-brand-primary transition-colors">
                    BOARD <span class="text-brand-primary">Algo</span>
                </span>
            </a>

            <div class="hidden lg:flex items-center space-x-8 text-sm font-medium text-slate-400">
                <a href="#features" class="hover:text-white transition-colors">Platform</a>
                <a href="#demo" class="hover:text-white transition-colors">How it Works</a>
                <a href="#parents" class="hover:text-white transition-colors">Parents</a>
            </div>

            <div class="flex items-center gap-4">
                <a href="/login" class="text-sm font-semibold text-slate-300 hover:text-white hidden sm:block">Log in</a>
                <a href="/signup" class="px-5 py-2.5 rounded-lg bg-brand-primary hover:bg-indigo-500 text-white font-semibold text-sm shadow-[0_0_20px_rgba(99,102,241,0.3)] transition-all">
                    Start Learning
                </a>
            </div>
        </div>
    </nav>

    <section class="relative pt-32 pb-20 px-6 min-h-screen flex items-center justify-center overflow-hidden">
        
        <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] bg-gradient-glow rounded-full pointer-events-none"></div>

        <div class="max-w-5xl mx-auto text-center relative z-10">
            
            <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full border border-brand-border bg-brand-surface/50 backdrop-blur-sm mb-8 section-fade hover:border-brand-primary/50 transition-colors cursor-default">
                <span class="flex h-2 w-2 relative">
                    <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-green-400 opacity-75"></span>
                    <span class="relative inline-flex rounded-full h-2 w-2 bg-green-500"></span>
                </span>
                <span class="text-xs font-bold uppercase tracking-wider text-slate-300">AI Model v2.0 Online</span>
            </div>

            <h1 class="text-5xl md:text-7xl lg:text-8xl font-display font-bold leading-tight mb-8 section-fade text-white">
                Predict Your <br class="hidden md:block" />
                <span id="typewriter" class="text-transparent bg-clip-text bg-gradient-to-r from-brand-primary to-brand-accent"></span><span class="text-brand-primary animate-cursor-blink">|</span>
            </h1>

            <p class="text-lg md:text-xl text-slate-400 mb-12 max-w-2xl mx-auto font-medium leading-relaxed section-fade">
                The only platform that predicts CBSE board questions with 85% accuracy. Evaluates like a strict invigilator. Forces you to score 99%.
            </p>

            <div class="flex flex-col sm:flex-row justify-center gap-4 section-fade w-full px-4 sm:px-0">
                <a href="/signup" class="w-full sm:w-auto px-8 py-4 rounded-xl bg-white text-brand-black font-bold text-lg hover:scale-105 transition-transform flex items-center justify-center gap-2 shadow-[0_0_20px_rgba(255,255,255,0.3)]">
                    Analyze My Syllabus
                    <i data-lucide="arrow-right" class="w-5 h-5 text-brand-primary"></i>
                </a>
                <a href="#demo" class="w-full sm:w-auto px-8 py-4 rounded-xl glass text-white border border-white/10 font-bold text-lg hover:bg-white/10 transition-all flex items-center justify-center gap-2 backdrop-blur-md">
                    <i data-lucide="play-circle" class="w-5 h-5"></i>
                    Watch Demo
                </a>
            </div>

            <div class="mt-20 pt-8 border-t border-white/5 section-fade">
                <p class="text-xs font-bold text-slate-600 uppercase tracking-widest mb-6">Trusted by students from</p>
                <div class="flex flex-wrap justify-center gap-6 md:gap-16 opacity-40 grayscale hover:grayscale-0 transition-all duration-500">
                    <div class="flex items-center gap-2 font-display font-bold text-slate-300 text-lg">DPS</div>
                    <div class="flex items-center gap-2 font-display font-bold text-slate-300 text-lg">KENDRIYA VIDYALAYA</div>
                    <div class="flex items-center gap-2 font-display font-bold text-slate-300 text-lg">RYAN INT.</div>
                    <div class="flex items-center gap-2 font-display font-bold text-slate-300 text-lg">D.A.V.</div>
                </div>
            </div>
        </div>
    </section>

    <section id="demo" class="py-24 relative z-10">
        <div class="max-w-7xl mx-auto px-6">
            <div class="text-center mb-16">
                <h2 class="text-3xl md:text-5xl font-display font-bold text-white mb-4">The Algorithm</h2>
                <p class="text-slate-400">From confusion to confidence in 3 steps.</p>
            </div>

            <div class="grid md:grid-cols-3 gap-6 relative">
                <div class="glass glow-card p-8 rounded-2xl relative group border border-white/5">
                    <div class="w-14 h-14 bg-brand-surface rounded-xl flex items-center justify-center mb-6 border border-brand-border group-hover:scale-110 transition-transform shadow-lg relative z-10">
                        <i data-lucide="scan-line" class="w-7 h-7 text-brand-primary"></i>
                    </div>
                    <h3 class="text-xl font-bold text-white mb-3 relative z-10">1. Snap & Scan</h3>
                    <p class="text-sm text-slate-400 relative z-10">Upload a photo of your question or handwritten answer. Our OCR digitizes it instantly.</p>
                </div>

                <div class="glass glow-card p-8 rounded-2xl relative group border border-white/5">
                    <div class="w-14 h-14 bg-brand-surface rounded-xl flex items-center justify-center mb-6 border border-brand-border group-hover:scale-110 transition-transform shadow-lg relative z-10">
                        <i data-lucide="cpu" class="w-7 h-7 text-brand-accent"></i>
                    </div>
                    <h3 class="text-xl font-bold text-white mb-3 relative z-10">2. AI Analysis</h3>
                    <p class="text-sm text-slate-400 relative z-10">Gemini checks your answer against 10 years of topper sheets and strict marking schemes.</p>
                </div>

                <div class="glass glow-card p-8 rounded-2xl relative group border border-white/5">
                    <div class="w-14 h-14 bg-brand-surface rounded-xl flex items-center justify-center mb-6 border border-brand-border group-hover:scale-110 transition-transform shadow-lg relative z-10">
                        <i data-lucide="trophy" class="w-7 h-7 text-green-500"></i>
                    </div>
                    <h3 class="text-xl font-bold text-white mb-3 relative z-10">3. Predict & Score</h3>
                    <p class="text-sm text-slate-400 relative z-10">Get a probability score for that question and a personalized plan to fix your weak spots.</p>
                </div>
            </div>
        </div>
    </section>

    <section id="features" class="py-24 px-6 relative z-10 border-t border-white/5">
        <div class="max-w-7xl mx-auto">
            <div class="text-center max-w-3xl mx-auto mb-20">
                <h2 class="text-brand-primary font-bold tracking-widest uppercase text-xs mb-3">Core Capabilities</h2>
                <h3 class="text-4xl font-display font-bold text-white mb-6">Built for the 99% Club</h3>
            </div>

            <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
                
                <div class="glass glow-card p-8 rounded-2xl group border border-white/5">
                    <i data-lucide="search" class="w-10 h-10 text-brand-primary mb-6 relative z-10"></i>
                    <h4 class="text-xl font-bold text-white mb-2 relative z-10">AI Solution Finder</h4>
                    <p class="text-slate-400 text-sm leading-relaxed relative z-10">
                        Don't just get the answer. Get the <strong>Topper's Version</strong> and the probability of it appearing in 2026.
                    </p>
                </div>

                <div class="glass glow-card p-8 rounded-2xl group border border-white/5">
                    <i data-lucide="bar-chart-2" class="w-10 h-10 text-brand-accent mb-6 relative z-10"></i>
                    <h4 class="text-xl font-bold text-white mb-2 relative z-10">Concept Predictor</h4>
                    <p class="text-slate-400 text-sm leading-relaxed relative z-10">
                        Enter a concept (e.g., "Saponification"). AI analyzes 10 years of PYQs to tell you if you should study it or skip it.
                    </p>
                </div>

                <div class="glass glow-card p-8 rounded-2xl group border border-white/5">
                    <i data-lucide="pen-tool" class="w-10 h-10 text-red-400 mb-6 relative z-10"></i>
                    <h4 class="text-xl font-bold text-white mb-2 relative z-10">Strict Board Examiner</h4>
                    <p class="text-slate-400 text-sm leading-relaxed relative z-10">
                        Upload your handwritten answer. AI grades it strictly based on the <strong>CBSE Marking Scheme</strong>.
                    </p>
                </div>

                <div class="glass glow-card p-8 rounded-2xl lg:col-span-2 flex flex-col md:flex-row items-center gap-8 border border-white/5">
                    <div class="flex-1 relative z-10">
                        <i data-lucide="file-text" class="w-10 h-10 text-green-400 mb-6"></i>
                        <h4 class="text-xl font-bold text-white mb-2">Personalized Question Bank</h4>
                        <p class="text-slate-400 text-sm leading-relaxed mb-6">
                            Generate a custom question paper based on YOUR weak areas. Solve it live or <strong>Download as PDF</strong> (with/without solutions).
                        </p>
                        <a href="/signup" class="inline-flex items-center gap-2 text-sm font-bold text-brand-primary hover:text-white transition-colors">
                            Generate Now <i data-lucide="arrow-right" class="w-4 h-4"></i>
                        </a>
                    </div>
                    <div class="w-full md:w-64 bg-brand-black/50 border border-brand-border rounded-lg p-4 relative z-10">
                        <div class="flex items-center gap-3 mb-3">
                            <div class="w-8 h-8 bg-red-500/20 rounded flex items-center justify-center"><i data-lucide="file" class="w-4 h-4 text-red-500"></i></div>
                            <div>
                                <div class="text-xs font-bold text-white">Physics_Ch1.pdf</div>
                                <div class="text-[10px] text-slate-500">1.2 MB • Generated Just now</div>
                            </div>
                        </div>
                        <div class="h-1 w-full bg-brand-border rounded-full overflow-hidden">
                            <div class="h-full w-full bg-brand-primary"></div>
                        </div>
                    </div>
                </div>

                <div class="glass glow-card p-8 rounded-2xl group border border-white/5">
                    <i data-lucide="brain-circuit" class="w-10 h-10 text-pink-400 mb-6 relative z-10"></i>
                    <h4 class="text-xl font-bold text-white mb-2 relative z-10">Mnemonics & Stories</h4>
                    <p class="text-slate-400 text-sm leading-relaxed relative z-10">
                        Convert tough concepts (like Periodic Table trends) into funny stories or mnemonics automatically.
                    </p>
                </div>
            </div>
        </div>
    </section>

    <footer class="border-t border-white/5 bg-brand-black pt-16 pb-8 relative z-10">
        <div class="max-w-7xl mx-auto px-6 grid grid-cols-1 md:grid-cols-4 gap-10 mb-12">
            
            <div class="md:col-span-1">
                <span class="text-2xl font-display font-bold text-white">BOARD <span class="text-brand-primary">Algo</span></span>
                <p class="text-slate-500 text-sm mt-4">Bangalore-based EdTech revolutionizing Class 10 preparation with AI.</p>
            </div>

            <div>
                <h5 class="font-bold text-white mb-4">Platform</h5>
                <ul class="space-y-3 text-sm text-slate-500">
                    <li><a href="#" class="hover:text-brand-primary">Solution Finder</a></li>
                    <li><a href="#" class="hover:text-brand-primary">Concept Predictor</a></li>
                    <li><a href="#" class="hover:text-brand-primary">Question Bank</a></li>
                </ul>
            </div>

            <div>
                <h5 class="font-bold text-white mb-4">Legal</h5>
                <ul class="space-y-3 text-sm text-slate-500">
                    <li><a href="#" class="hover:text-brand-primary">Privacy Policy</a></li>
                    <li><a href="#" class="hover:text-brand-primary">Terms of Service</a></li>
                </ul>
            </div>

            <div class="md:col-span-1">
                <div class="p-4 rounded-xl bg-brand-surface border border-brand-border text-center">
                    <p class="text-xs font-bold text-slate-500 mb-2">PRODUCT BY</p>
                    <a href="https://xceedjs.com" class="text-lg font-bold text-white hover:text-brand-primary transition-colors">XCEEDJS</a>
                </div>
            </div>

        </div>
        
        <div class="max-w-7xl mx-auto px-6 border-t border-white/5 pt-8 flex flex-col md:flex-row justify-between items-center gap-4 text-sm text-slate-600">
            <p>&copy; 2026 XCEEDJS. All rights reserved.</p>
            <p>Made with &hearts; in Bangalore, India.</p>
        </div>
    </footer>

    <script>
        // 1. ICONS
        lucide.createIcons();

        // 2. LOGO HANDLING
        const imgLogo = document.getElementById('img-logo');
        const textLogo = document.getElementById('text-logo');
        imgLogo.onload = function() { imgLogo.classList.remove('hidden'); textLogo.classList.add('hidden'); };
        imgLogo.onerror = function() { imgLogo.classList.add('hidden'); textLogo.classList.remove('hidden'); };

        // 3. SMOOTH LOADER ANIMATION
        window.addEventListener('load', () => {
            const tl = gsap.timeline();
            tl.to("#loader-bar", { width: "100%", duration: 1.2, ease: "power2.inOut" }, 0)
              .to("#loader-percent", { innerHTML: "100%", duration: 1.2, snap: { innerHTML: 1 }, onUpdate: function() { this.targets()[0].innerHTML += "%"; } }, 0)
              .to("#loader", { yPercent: -100, duration: 0.8, ease: "power4.inOut", delay: 0.1 })
              .from(".section-fade", { y: 30, opacity: 0, duration: 0.8, stagger: 0.1, ease: "power2.out" }, "-=0.4");
        });

        // 4. REAL TYPEWRITER EFFECT
        const textElement = document.getElementById('typewriter');
        const phrases = ["Board Score", "Weak Areas", "Future Stream"];
        let phraseIndex = 0;
        let charIndex = 0;
        let isDeleting = false;

        function type() {
            const currentPhrase = phrases[phraseIndex];
            
            if (isDeleting) {
                textElement.innerText = currentPhrase.substring(0, charIndex - 1);
                charIndex--;
            } else {
                textElement.innerText = currentPhrase.substring(0, charIndex + 1);
                charIndex++;
            }

            let typeSpeed = isDeleting ? 50 : 100;

            if (!isDeleting && charIndex === currentPhrase.length) {
                isDeleting = true;
                typeSpeed = 2000; // Pause at end
            } else if (isDeleting && charIndex === 0) {
                isDeleting = false;
                phraseIndex = (phraseIndex + 1) % phrases.length;
                typeSpeed = 500; // Pause before typing next
            }

            setTimeout(type, typeSpeed);
        }

        // Start typing loop after loader
        setTimeout(type, 2000);

        // 5. 3D BACKGROUND
        const initThreeJS = () => {
            const container = document.getElementById('canvas-container');
            const scene = new THREE.Scene();
            scene.fog = new THREE.FogExp2(0x020617, 0.03);

            const camera = new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight, 0.1, 1000);
            camera.position.set(0, 3, 10);
            camera.rotation.x = -0.2;

            const renderer = new THREE.WebGLRenderer({ alpha: true, antialias: true });
            renderer.setSize(window.innerWidth, window.innerHeight);
            renderer.setPixelRatio(window.devicePixelRatio);
            container.appendChild(renderer.domElement);

            // Infinite Grid
            const gridGeo = new THREE.PlaneGeometry(200, 200, 60, 60);
            const gridMat = new THREE.MeshBasicMaterial({ 
                color: 0x6366f1, 
                wireframe: true, 
                transparent: true, 
                opacity: 0.12 
            });
            const grid = new THREE.Mesh(gridGeo, gridMat);
            grid.rotation.x = -Math.PI / 2;
            scene.add(grid);

            // Particles
            const particlesGeo = new THREE.BufferGeometry();
            const particlesCount = 300;
            const posArray = new Float32Array(particlesCount * 3);
            for(let i=0; i < particlesCount * 3; i++) {
                posArray[i] = (Math.random() - 0.5) * 60;
            }
            particlesGeo.setAttribute('position', new THREE.BufferAttribute(posArray, 3));
            const particlesMat = new THREE.PointsMaterial({ size: 0.08, color: 0xffffff, transparent: true, opacity: 0.4 });
            const particlesMesh = new THREE.Points(particlesGeo, particlesMat);
            scene.add(particlesMesh);

            let time = 0;
            const animate = () => {
                requestAnimationFrame(animate);
                time += 0.003;
                grid.position.z = (time * 10) % 5;
                const positionAttribute = gridGeo.attributes.position;
                for (let i = 0; i < positionAttribute.count; i++) {
                    const z = positionAttribute.getZ(i);
                    const wave = Math.sin(i / 5 + time * 5) * 0.2; 
                    positionAttribute.setZ(i, wave); 
                }
                positionAttribute.needsUpdate = true;
                renderer.render(scene, camera);
            };
            animate();
            window.addEventListener('resize', () => {
                camera.aspect = window.innerWidth / window.innerHeight;
                camera.updateProjectionMatrix();
                renderer.setSize(window.innerWidth, window.innerHeight);
            });
        };
        initThreeJS();

        // 6. MOUSE GLOW
        const cards = document.querySelectorAll('.glow-card');
        cards.forEach(card => {
            card.addEventListener('mousemove', e => {
                const rect = card.getBoundingClientRect();
                card.style.setProperty('--x', `${e.clientX - rect.left}px`);
                card.style.setProperty('--y', `${e.clientY - rect.top}px`);
            });
        });

        // 7. SCROLL ANIMATION
        gsap.utils.toArray('.glass').forEach(panel => {
            gsap.from(panel, {
                scrollTrigger: { trigger: panel, start: "top 85%" },
                y: 40, opacity: 0, duration: 0.8, ease: "power2.out"
            });
        });
    </script>
</body>
</html>